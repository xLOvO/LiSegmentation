# model_data

此目录用于存放模型权重文件。

常用文件：

- `mobilenet_v2.pth.tar`：MobileNetV2 backbone 预训练权重。
- `xception_pytorch_imagenet.pth`：Xception backbone 预训练权重。
- `deeplab_mobilenetv2.pth`：可选的 DeepLabV3+ 初始化权重。

训练完成后的权重默认保存在项目根目录的 `logs/` 下。
